using math ;

square n = n*n ;

a::matrix + b::matrix = zipwith (+) a b ;

a::matrix - b::matrix = zipwith (-) a b ;

v::matrix * n = map (*n) v ;

v::matrix / n = map (/n) v ;

norm v = sqrt ( square ( v!0 ) + square ( v!1 ) ) ;

normalize v = v / (norm v) ;
