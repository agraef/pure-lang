
/* This directory contains a few basic pd-pure examples which are all
   demonstrated in the pure-help.pd patch. This example shows a simple object
   with a creation argument. */

// Note that the --required option is needed if the object is to be compiled
// to native code.
#! --required add
add x y = x+y;
