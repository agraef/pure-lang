// Custom version of ccrider which lets you automate all 128 MIDI CCs. This
// illustrates how to create custom versions of ccrider by just redefining
// some of the customizable configuration values of the generic version in
// ccrider.pure.

let nparams = 128;
let inccs = 0..nparams-1;
let outccs = 0..nparams-1;

// We just import the generic definition of ccrider here, which takes care of
// all the rest.
using ccrider;
