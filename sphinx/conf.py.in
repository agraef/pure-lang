# -*- coding: utf-8 -*-
#
# Pure documentation build configuration file, created by
# sphinx-quickstart on Tue Nov  2 11:13:17 2010.
#
# This file is execfile()d with the current directory set to its containing dir.
#
# Note that not all possible configuration values are present in this
# autogenerated file.
#
# All configuration values have a default; values that are commented out
# serve to show the default.

import sys, os, time

# If extensions (or modules to document with autodoc) are in another directory,
# add these directories to sys.path here. If the directory is relative to the
# documentation root, use os.path.abspath to make it absolute, like shown here.
#sys.path.insert(0, os.path.abspath('.'))

# -- General configuration -----------------------------------------------------

# If your documentation needs a minimal Sphinx version, state it here.
#needs_sphinx = '1.0'

# Add any Sphinx extension module names here, as strings. They can be extensions
# coming with Sphinx (named 'sphinx.ext.*') or your custom ones.
extensions = ['sphinxcontrib.puredomain', 'sphinx.ext.pngmath']

# Add any paths that contain templates here, relative to this directory.
templates_path = ['templates']

# The suffix of source filenames.
source_suffix = '.txt'

# The encoding of source files.
#source_encoding = 'utf-8-sig'

# The master toctree document.
master_doc = 'index'

# General information about the project.
project = u'Pure'
copyright = u'2009-%s, Albert Gräf et al' % time.strftime('%Y')

# The version info for the project you're documenting, acts as replacement for
# |version| and |release|, also used in various other places throughout the
# built documents.
#
# The short X.Y version.
version = "@version@"
# The full version, including alpha/beta/rc tags.
release = "@version@"

# The language for content autogenerated by Sphinx. Refer to documentation
# for a list of supported languages.
#language = None

# There are two options for replacing |today|: either, you set today to some
# non-false value, then it is used:
#today = ''
# Else, today_fmt is used as the format for a strftime call.
#today_fmt = '%B %d, %Y'

# List of patterns, relative to source directory, that match files and
# directories to ignore when looking for source files.
exclude_patterns = []

# The reST default role (used for this markup: `text`) to use for all documents.
#default_role = None

# If true, '()' will be appended to :func: etc. cross-reference text.
#add_function_parentheses = True

# If true, the current module name will be prepended to all description
# unit titles (such as .. function::).
#add_module_names = True

# If true, sectionauthor and moduleauthor directives will be shown in the
# output. They are ignored by default.
#show_authors = False

# The name of the Pygments (syntax highlighting) style to use.
#pygments_style = 'none'
#pygments_style = 'sphinx'
#pygments_style = 'pygments.styles.bw.BlackWhiteStyle'

# The default highlighting language.
#highlight_language = 'none'
# Uncomment this if you have the Pure lexer for Pygment.
highlight_language = 'pure'

# A list of ignored prefixes for module index sorting.
#modindex_common_prefix = []


# -- Options for HTML output ---------------------------------------------------

# The theme to use for HTML and HTML Help pages.  See the documentation for
# a list of builtin themes.
html_theme = 'pydoctheme'

# Theme options are theme-specific and customize the look and feel of a theme
# further.  For a list of options available for each theme, see the
# documentation.
#html_theme_options = {}
html_theme_options = {
    "collapsiblesidebar": "true",
#    "stickysidebar": "true",
#    "relbarbgcolor": "black"
}

# Add any paths that contain custom themes here, relative to this directory.
#html_theme_path = []
html_theme_path = ["themes"]

# The name for this set of Sphinx documents.  If None, it defaults to
# "<project> v<release> documentation".
html_title = 'Pure Language and Library Documentation'

# A shorter title for the navigation bar.  Default is the same as html_title.
#html_short_title = None

# The name of an image file (relative to this directory) to place at the top
# of the sidebar.
#html_logo = None

# The name of an image file (within the static path) to use as favicon of the
# docs.  This file should be a Windows icon file (.ico) being 16x16 or 32x32
# pixels large.
#html_favicon = None

# Add any paths that contain custom static files (such as style sheets) here,
# relative to this directory. They are copied after the builtin static files,
# so a file named "default.css" will overwrite the builtin "default.css".
html_static_path = ["static"]

# If not '', a 'Last updated on:' timestamp is inserted at every page bottom,
# using the given strftime format.
#html_last_updated_fmt = '%b %d, %Y'
html_last_updated_fmt = '%b %d, %Y'

# If true, SmartyPants will be used to convert quotes and dashes to
# typographically correct entities.
#html_use_smartypants = True

# Custom sidebar templates, maps document names to template names.
#html_sidebars = {}

# Additional templates that should be rendered to pages, maps page names to
# template names.
#html_additional_pages = {}

# If false, no module index is generated.
#html_domain_indices = True

# If false, no index is generated.
#html_use_index = True

# If true, the index is split into individual pages for each letter.
#html_split_index = False

# If true, links to the reST sources are added to the pages.
#html_show_sourcelink = True

# If true, "Created using Sphinx" is shown in the HTML footer. Default is True.
#html_show_sphinx = True

# If true, "(C) Copyright ..." is shown in the HTML footer. Default is True.
#html_show_copyright = True

# If true, an OpenSearch description file will be output, and all pages will
# contain a <link> tag referring to it.  The value of this option must be the
# base URL from which the finished HTML is served.
#html_use_opensearch = ''

# This is the file name suffix for HTML files (e.g. ".xhtml").
#html_file_suffix = None

# Output file base name for HTML help builder.
htmlhelp_basename = 'puredoc'


# -- Options for LaTeX output --------------------------------------------------

# The paper size ('letter' or 'a4').
latex_paper_size = 'a4'

# The font size ('10pt', '11pt' or '12pt').
latex_font_size = '11pt'

# Grouping the document tree into LaTeX files. List of tuples
# (source start file, target name, title, author, documentclass [howto/manual]).
latex_documents = [
  ('index', 'puredoc.tex', u'Pure Language and Library Documentation',
   u'Albert Gräf (Editor)', 'manual'),
]

# The name of an image file (relative to this directory) to place at the top of
# the title page.
latex_logo = u"waterdrop.png"

# For "manual" documents, if this is true, then toplevel headings are parts,
# not chapters.
#latex_use_parts = True

# If true, show page references after internal links.
#latex_show_pagerefs = False

# If true, show URL addresses after external links.
#latex_show_urls = False

# Documents to append as an appendix to all manuals.
#latex_appendices = []

# If false, no module index is generated.
#latex_domain_indices = True

latex_elements = {'inputenc': '\\usepackage[utf8x]{inputenc}', 'preamble': '\\input{preamble.ltx}','fncychap': ''}

latex_additional_files = [u"preamble.ltx"]

# -- Options for manual page output --------------------------------------------

# One entry per manual page. List of tuples
# (source start file, name, description, authors, manual section).
man_pages = [
    ('index', 'puredoc', u'Pure Language and Library Documentation',
     [u'Albert Gräf (Editor)'], 1)
]


# -- Options for texinfo output ------------------------------------------------

# One entry per texinfo file. List of tuples
# (source start file, name, title, authors, dir entry, description, category, toctree only).
texinfo_documents = [
    ('pure', 'pure', 'The Pure Manual', u'Albert Gräf (Editor)', 'pure',
     'The Pure Manual', 'Pure Language and Library Documentation', False),
    ('purelib', 'purelib', 'Pure Library Manual', u'Albert Gräf (Editor)',
     'purelib', 'Pure Library Manual', 'Pure Language and Library Documentation',
     False),
    ('pure-avahi', 'pure-avahi', 'pure-avahi', u'Albert Gräf (Editor)',
     'pure-avahi', 'pure-avahi: Pure Avahi Interface', 'Pure Language and Library Documentation',
     False),
    ('pure-doc', 'pure-doc', 'pure-doc', u'Albert Gräf (Editor)',
     'pure-doc', 'pure-doc', 'Pure Language and Library Documentation',
     False),
    ('pure-ffi', 'pure-ffi', 'pure-ffi', u'Albert Gräf (Editor)',
     'pure-ffi', 'pure-ffi', 'Pure Language and Library Documentation',
     False),
    ('pure-gen', 'pure-gen', 'pure-gen', u'Albert Gräf (Editor)',
     'pure-gen', 'pure-gen: Pure interface generator', 'Pure Language and Library Documentation',
     False),
    ('pure-readline', 'pure-readline', 'pure-readline', u'Albert Gräf (Editor)',
     'pure-readline', 'pure-readline', 'Pure Language and Library Documentation',
     False),
    ('pure-sockets', 'pure-sockets', 'pure-sockets', u'Albert Gräf (Editor)',
     'pure-sockets', 'pure-sockets: Pure Sockets Interface', 'Pure Language and Library Documentation',
     False),
    ('pure-stldict', 'pure-stldict', 'pure-stldict', u'Albert Gräf (Editor)',
     'pure-stldict', 'pure-stldict', 'Pure Language and Library Documentation',
     False),
    ('pure-stlmap', 'pure-stlmap', 'pure-stlmap', u'Albert Gräf (Editor)',
     'pure-stlmap', 'pure-stlmap', 'Pure Language and Library Documentation',
     False),
    ('pure-stlvec', 'pure-stlvec', 'pure-stlvec', u'Albert Gräf (Editor)',
     'pure-stlvec', 'pure-stlvec', 'Pure Language and Library Documentation',
     False),
    ('gnumeric-pure', 'gnumeric-pure', 'gnumeric-pure', u'Albert Gräf (Editor)',
     'gnumeric-pure', 'Gnumeric/Pure: A Pure Plugin for Gnumeric', 'Pure Language and Library Documentation',
     False),
    ('pure-glpk', 'pure-glpk', 'pure-glpk', u'Albert Gräf (Editor)',
     'pure-glpk', 'Pure-GLPK - GLPK interface for the Pure programming language', 'Pure Language and Library Documentation',
     False),
    ('pure-gplot', 'pure-gplot', 'pure-gplot', u'Albert Gräf (Editor)',
     'pure-gplot', 'Gnuplot bindings', 'Pure Language and Library Documentation',
     False),
    ('pure-gsl', 'pure-gsl', 'pure-gsl', u'Albert Gräf (Editor)',
     'pure-gsl', 'pure-gsl - GNU Scientific Library Interface for Pure', 'Pure Language and Library Documentation',
     False),
    ('pure-mpfr', 'pure-mpfr', 'pure-mpfr', u'Albert Gräf (Editor)',
     'pure-mpfr', 'pure-mpfr', 'Pure Language and Library Documentation',
     False),
    ('pure-octave', 'pure-octave', 'pure-octave', u'Albert Gräf (Editor)',
     'pure-octave', 'pure-octave', 'Pure Language and Library Documentation',
     False),
    ('pure-rational', 'pure-rational', 'pure-rational', u'Albert Gräf (Editor)',
     'pure-rational', 'Pure-Rational - Rational number library', 'Pure Language and Library Documentation',
     False),
    ('pure-reduce', 'pure-reduce', 'pure-reduce', u'Albert Gräf (Editor)',
     'pure-reduce', 'A REDUCE Interface for the Pure Programming Language', 'Pure Language and Library Documentation',
     False),
    ('pure-csv', 'pure-csv', 'pure-csv', u'Albert Gräf (Editor)',
     'pure-csv', 'Pure-CSV - Comma Separated Value Interface', 'Pure Language and Library Documentation',
     False),
    ('pure-fastcgi', 'pure-fastcgi', 'pure-fastcgi', u'Albert Gräf (Editor)',
     'pure-fastcgi', 'pure-fastcgi: FastCGI module for Pure', 'Pure Language and Library Documentation',
     False),
    ('pure-odbc', 'pure-odbc', 'pure-odbc', u'Albert Gräf (Editor)',
     'pure-odbc', 'Pure-ODBC - ODBC interface', 'Pure Language and Library Documentation',
     False),
    ('pure-sql3', 'pure-sql3', 'pure-sql3', u'Albert Gräf (Editor)',
     'pure-sql3', 'pure-sql3', 'Pure Language and Library Documentation',
     False),
    ('pure-xml', 'pure-xml', 'pure-xml', u'Albert Gräf (Editor)',
     'pure-xml', 'Pure-XML - XML/XSLT interface', 'Pure Language and Library Documentation',
     False),
    ('pure-g2', 'pure-g2', 'pure-g2', u'Albert Gräf (Editor)',
     'pure-g2', 'pure-g2', 'Pure Language and Library Documentation',
     False),
    ('pure-gl', 'pure-gl', 'pure-gl', u'Albert Gräf (Editor)',
     'pure-gl', 'Pure OpenGL Bindings', 'Pure Language and Library Documentation',
     False),
    ('pure-gtk', 'pure-gtk', 'pure-gtk', u'Albert Gräf (Editor)',
     'pure-gtk', 'Pure GTK+ Bindings', 'Pure Language and Library Documentation',
     False),
    ('pure-tk', 'pure-tk', 'pure-tk', u'Albert Gräf (Editor)',
     'pure-tk', 'pure-tk', 'Pure Language and Library Documentation',
     False),
    ('faust2pd', 'faust2pd', 'faust2pd', u'Albert Gräf (Editor)',
     'faust2pd', 'faust2pd: Pd Patch Generator for Faust', 'Pure Language and Library Documentation',
     False),
    ('pd-faust', 'pd-faust', 'pd-faust', u'Albert Gräf (Editor)',
     'pd-faust', 'pd-faust', 'Pure Language and Library Documentation',
     False),
    ('pd-pure', 'pd-pure', 'pd-pure', u'Albert Gräf (Editor)',
     'pd-pure', 'pd-pure: Pd loader for Pure scripts', 'Pure Language and Library Documentation',
     False),
    ('pure-audio', 'pure-audio', 'pure-audio', u'Albert Gräf (Editor)',
     'pure-audio', 'pure-audio', 'Pure Language and Library Documentation',
     False),
    ('pure-faust', 'pure-faust', 'pure-faust', u'Albert Gräf (Editor)',
     'pure-faust', 'pure-faust: Run Faust programs in Pure scripts', 'Pure Language and Library Documentation',
     False),
    ('pure-liblo', 'pure-liblo', 'pure-liblo', u'Albert Gräf (Editor)',
     'pure-liblo', 'pure-liblo', 'Pure Language and Library Documentation',
     False),
    ('pure-midi', 'pure-midi', 'pure-midi', u'Albert Gräf (Editor)',
     'pure-midi', 'pure-midi', 'Pure Language and Library Documentation',
     False),
]


# -- Options for Epub output ---------------------------------------------------

# Bibliographic Dublin Core info.
epub_title = u'Pure Language and Library Documentation'
epub_author = u'Albert Gräf (Editor)'
epub_publisher = u'Albert Gräf'
epub_copyright = u'2009-2010, Albert Gräf et al'

# The language of the text. It defaults to the language option
# or en if the language is not set.
#epub_language = ''

# The scheme of the identifier. Typical schemes are ISBN or URL.
#epub_scheme = ''

# The unique identifier of the text. This can be a ISBN number
# or the project homepage.
#epub_identifier = ''

# A unique identification for the text.
#epub_uid = ''

# HTML files that should be inserted before the pages created by sphinx.
# The format is a list of tuples containing the path and title.
#epub_pre_files = []

# HTML files shat should be inserted after the pages created by sphinx.
# The format is a list of tuples containing the path and title.
#epub_post_files = []

# A list of files that should not be packed into the epub file.
#epub_exclude_files = []

# The depth of the table of contents in toc.ncx.
#epub_tocdepth = 3

# Allow duplicate toc entries.
#epub_tocdup = True
