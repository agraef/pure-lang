
/* Symbolic evaluation example with local rule sets. */

expand = reduce with
  (a+b)*c = a*c+b*c;
  a*(b+c) = a*b+a*c;
end;

factor = reduce with
  a*c+b*c1 = (a+b)*c if c===c1;
  a*b+a1*c = a*(b+c) if a===a1;
end;

/* Example: */

expand ((a+b)*2);
factor (a*2+b*2);
