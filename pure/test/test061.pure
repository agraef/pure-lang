// Quoted specials and special patterns (Pure 0.47+).

'(\x->x+1);

foo = '(\x->x+1);
foo;

bar (\x y -> z) = '(\y x -> z);
bar $ '(\a b -> a-b);

baz x = '(\x->x+1);
baz y;

//def f (\x y -> z) = '(\y x -> z);
//f (\a b -> a-b);

let h = '(case 99 of x::int = x+1 if x>0 end);
h; eval h;
let h = '(x+1 when x::int = 99 end);
h; eval h;
let h = '(f 99 with f x::int = x+1 end);
h; eval h;
