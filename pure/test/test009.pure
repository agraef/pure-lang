fib2 n = a when a, b = fibs n end
            with fibs n = 0, 1 if n<=0;
                        = b, a+b when a, b = fibs (n-1) end
                          otherwise;
            end;
fib2 n = a when a, b = fibs n end
            with fibs n = 0, 1 if n<=0;
                        = b, a+b when a, b = fibs (n-1) end
                          otherwise;
            end;
map fib2 (1..10);
